The collection was rebuilt as a field archive. I added search, sorting, and grid controls so users could find previous scans by name, date, or tags, then switch the view depending on how much visual detail they needed. The detector gives live magnetic readings with a simple status system, so users can understand the result before reading raw sensor data.